# =============================================================================
# Cart Service — Deployment + Service
# =============================================================================
# Single-replica Flask API managing shopping carts and checkout.
# Tuned for minimal resource usage on an i3 PC.
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: ecommerce-real
  labels:
    app: cart-service
    app.kubernetes.io/part-of: deception-system
    app.kubernetes.io/component: cart-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
        app.kubernetes.io/part-of: deception-system
        app.kubernetes.io/component: cart-service
    spec:
      containers:
        - name: cart-service
          image: deception/cart-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
              protocol: TCP
          env:
            - name: DB_HOST
              value: postgres.ecommerce-real.svc.cluster.local
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: ecommerce
            - name: DB_USER
              value: appuser
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: 75m
              memory: 48Mi
            limits:
              cpu: 150m
              memory: 96Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
---
# =============================================================================
# Service — ClusterIP on port 8082
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: ecommerce-real
  labels:
    app: cart-service
    app.kubernetes.io/part-of: deception-system
    app.kubernetes.io/component: cart-service
spec:
  type: ClusterIP
  selector:
    app: cart-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
